// todo [after release]: error handling
// eslint-disable-next-line no-console
export const firebaseErrorHandler = (e: unknown) => console.error(e);

// eslint-disable-next-line no-console
export const defaultErrorHandler = (message: string) => console.error(message);
