// Firebase
// Interfaces
import { ICategory, IColumn } from '../interfaces';
// Utils

export const addCategory = async (
  title: string,
  column: IColumn,
): Promise<ICategory | null> => {
  return null;
};
